# URL of the FastAPI backend (no trailing slash)
# Local dev:    leave empty — Next.js rewrites /api/* → http://localhost:8000/api/*
# Vercel deploy: set to your Railway / Render / Fly backend URL
NEXT_PUBLIC_API_URL=
